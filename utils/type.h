#ifndef QUICKIM_TYPE_H
#define QUICKIM_TYPE_H

enum Type
{
    Login, Exit, Single, Multi, All, Key
};

enum ResponseType
{
    Refused, Server, Client, Broadcast, Multicast, File, Warn, PubKey
};

#endif
